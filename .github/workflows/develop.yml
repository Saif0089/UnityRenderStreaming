name: Develop - Deployment

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: Delta-mesh
    environment: develop
    env:
      SERVICE_NAME: webapp.service
      SERVICE_PORT: ${{secrets.DEVELOP_PORT}}
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        clean: false

    - name: Start WebApp
      run: |
        ls
        find . -name "*.sh" -exec chmod +x {} \;
        ./test_webapp.sh $SERVICE_PORT
        cat webapp.log